Samosa King premium Android app. UI is embedded directly in MainActivity.java so no Android asset folder is required. Firebase COD orders + GPS + live status are retained. Build: gradle assembleDebug.